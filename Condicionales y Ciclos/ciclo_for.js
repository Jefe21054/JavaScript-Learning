/**
 * CICLOS
 * BUCLES O LOOPS
 */

/**
 * SINTAXIS FOR
 * for (valor de inic; condicion; incr./decr. valor) {
 *      accion que se repite
 * }
 */

let texto = 'ivaniglesiasjavascript'

for (let i = 0; i < texto.length; i++) {
    console.log(texto[i])
}
